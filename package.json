{
  "name": "dpp-event",
  "version": "0.1.0",
  "description": "DPP Event Log Service: EPCIS to IOTA notarization pipeline",
  "main": "dist/server.js",
  "scripts": {
    "build": "npm run build:frontend && npx tsc",
    "build:frontend": "cd frontend && npm ci && npm run build",
    "serve": "node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "test": "tsx --test src/validate.test.ts src/hash.test.ts src/classify.test.ts src/transform.test.ts src/corsConfig.test.ts src/wwwRedirect.test.ts src/app.test.ts",
    "smoke": "tsx src/smoke.ts",
    "clear:graph": "tsx scripts/clear-graph.ts",
    "clear:build": "rm -rf dist frontend/dist frontend/node_modules/.vite",
    "clear:cache": "npm run clear:graph && npm run clear:build"
  },
  "dependencies": {
    "@comunica/query-sparql": "^5.1.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonld": "^8.3.3",
    "n3": "^1.21.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jsonld": "^1.5.15",
    "@types/supertest": "^7.2.0",
    "supertest": "^7.2.2",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=22"
  }
}
